# 辐射拾荒 · RadScav —— 投递 Demo

> 为「心动 / TapTap 制造 · AI 游戏创作实习生」岗位制作的小游戏 demo
> 制作日期：2026-08-21
> 技术：纯 HTML5 + Canvas + 原生 JS，**零依赖、单文件可玩**

---

## 怎么运行（最简单）

1. 进入本文件夹 `demo/`
2. **双击 `index.html`**，浏览器自动打开即玩
3. 方向键 / WASD 移动，收集黄色废料，避开红色辐射区

（无需安装任何东西、无需联网、无需构建）

---

## 给 HR 的回复文案（可直接复制）

> 王女士您好，关于您要的 demo：我做了一个极简单屏小游戏《辐射拾荒 RadScav》（纯 HTML5 + Canvas，零依赖，浏览器直接打开即玩，已附文件夹 / 链接）。
>
> 核心循环我设计成「**探索 → 风险决策 → 回报**」：玩家在辐射区持续扩大的地图里收集废料，越靠近危险区废料越密集、但停留持续掉血——用最小资源验证经典的"风险/收益比"玩法循环。
>
> 技术上用 AI 辅助编写，我主要负责**玩法拆解与体验打磨**（开始界面说明、HUD、血量梯度变色、重开闭环），只专注于"懂游戏的人怎么定义好玩"。
>
> 实习时间我之前已报：2026 年 9 月 7 日起到岗、每周 4 天、3 个月起可延续。麻烦您有空看下，期待进一步沟通，谢谢！

---

## 设计意图（给面试官看"懂游戏"的证据）

| 维度 | 做法 |
| --- | --- |
| 核心循环 | 探索（移动找废料）→ 风险决策（进危险区 vs 保守）→ 回报（分数），60 秒 / 血量归零结束 |
| 风险/收益比 | 废料偏向危险区边缘生成，越冒险收得越多，但掉血；强制玩家做权衡 |
| 体验打磨 | 开始界面一句话讲清循环；血量条按 40/20 阈值变色；危险区透明度随半径加深；结束界面显示得分 + 循环总结；一键重开 |
| AI 辅助开发 | 用 AI 辅助生成与检查代码，我聚焦玩法定义与体验细节（对应岗位"自然语言描述创意、AI 生成代码、人打磨玩法"） |

---

## 技术栈与结构

```
demo/
├── index.html   # 结构 + 样式 + 开始/结束遮罩
├── core.js      # 纯逻辑层（碰撞 / 危险区增长 / 掉血梯度），可单元测试
├── game.js      # 渲染与交互层（Canvas + 键盘输入 + 游戏循环）
└── test.cjs     # 核心逻辑单元测试（node test.cjs）
```

- 渲染：Canvas 2D，`requestAnimationFrame` 驱动，固定步长防卡顿
- 输入：方向键 / WASD，归一化对角线速度
- 测试：`node test.cjs` 已通过（碰撞、危险区封顶、掉血梯度断言）

---

## 怎么生成一个可分享链接（二选一）

**方案 A · 作为附件（最快）**
把 `demo/` 文件夹压缩成 `radscav.zip`，直接作为邮件 / 聊天附件发给 HR。

**方案 B · 部署到 GitHub Pages（更正式，有公开链接）**
1. 在 GitHub 新建仓库（如 `radscav`），把 `demo/` 内容 push 上去
2. Settings → Pages → 选 `main` 分支根目录 → 保存
3. 几分钟后获得 `https://<用户名>.github.io/radscav/` 公开链接，写进回复文案

> 你的 GitHub 账号已在本机连接，需要的话我可以帮你把仓库建好并 push。

---

## 更新日志

- **v1.1（2026-08-21）新增回血道具**：场上保持 2 个绿色医疗包，拾取 +25 血（封顶 100）；偏向安全区生成，强化"冒险进危险区 → 回安全区补血"的循环，让核心循环的"风险决策"更有层次。

## 已知边界（诚实标注）
- 单屏、单资源、单威胁，刻意做减法，只为干净地展示"核心循环"，不是成品游戏
- 未做音效、关卡、敌人 AI（按你"怎么简单怎么来"的要求省略）
